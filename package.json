{
  "name": "pwn-request-poc",
  "version": "1.0.0",
  "private": true,
  "description": "Intentionally vulnerable sandbox for validating pull_request_target exploit paths.",
  "scripts": {
    "build": "echo 'base branch build ok'"
  }
}
